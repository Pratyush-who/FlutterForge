name: flutterforge_cli
description: "FlutterForge CLI is a smart Dart command-line tool that helps Flutter developers create clean project structures, apply best architecture patterns, and get AI-powered package recommendations using Gemini AI."
version: 0.0.2
repository: https://github.com/Pratyush-who/flutterforge
homepage: https://pub.dev/packages/flutterforge

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.5.0
  process_run: ^1.0.0
  args: ^2.4.2
  yaml: ^3.1.2
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
